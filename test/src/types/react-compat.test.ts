/**
 * Testes dos tipos reativos
 */

describe('React Compat Types', () => {
  it('define tipos React', () => {
    expect(true).toBe(true);
  });

  it('compatibiliza com React', () => {
    expect(true).toBe(true);
  });
});
