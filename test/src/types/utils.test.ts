/**
 * Testes dos tipos utilitários
 */

describe('Utils Types', () => {
  it('define tipos utilitários', () => {
    expect(true).toBe(true);
  });

  it('fornece helpers de tipo', () => {
    expect(true).toBe(true);
  });
});
