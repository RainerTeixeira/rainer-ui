/**
 * Testes dos tipos de adapter
 */

describe('Adapter Types', () => {
  it('define tipos corretamente', () => {
    expect(true).toBe(true);
  });

  it('exporta interfaces necessárias', () => {
    expect(true).toBe(true);
  });
});
