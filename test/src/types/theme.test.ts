/**
 * Testes dos tipos de tema
 */

describe('Theme Types', () => {
  it('define tipos de tema corretamente', () => {
    expect(true).toBe(true);
  });

  it('exporta interfaces de tema', () => {
    expect(true).toBe(true);
  });
});
