/**
 * Testes dos tipos de tokens
 */

describe('Tokens Types', () => {
  it('define tipos de tokens corretamente', () => {
    expect(true).toBe(true);
  });

  it('exporta interfaces de tokens', () => {
    expect(true).toBe(true);
  });
});
