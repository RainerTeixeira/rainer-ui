/**
 * Testes do componente IconButton
 */

describe('IconButton', () => {
  it('renderiza corretamente', () => {
    expect(true).toBe(true);
  });

  it('exibe apenas ícone', () => {
    expect(true).toBe(true);
  });

  it('executa ação ao clicar', () => {
    expect(true).toBe(true);
  });
});
