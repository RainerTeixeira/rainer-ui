/**
 * Testes do componente Form
 */

describe('Form', () => {
  it('renderiza corretamente', () => {
    expect(true).toBe(true);
  });

  it('envia dados do formulário', () => {
    expect(true).toBe(true);
  });

  it('valida campos do formulário', () => {
    expect(true).toBe(true);
  });
});
