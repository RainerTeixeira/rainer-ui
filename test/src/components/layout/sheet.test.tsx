/**
 * Testes do componente Sheet
 */

describe('Sheet', () => {
  it('renderiza corretamente', () => {
    expect(true).toBe(true);
  });

  it('exibe painel deslizante', () => {
    expect(true).toBe(true);
  });

  it('permite abrir e fechar', () => {
    expect(true).toBe(true);
  });
});
