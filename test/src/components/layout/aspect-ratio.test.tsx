/**
 * Testes do componente AspectRatio
 */

describe('AspectRatio', () => {
  it('renderiza corretamente', () => {
    expect(true).toBe(true);
  });

  it('mantém proporção do aspecto', () => {
    expect(true).toBe(true);
  });
});
