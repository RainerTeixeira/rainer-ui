/**
 * Testes do hook useCarouselKeyboard
 */

describe('useCarouselKeyboard', () => {
  it('retorna funções de navegação', () => {
    expect(true).toBe(true);
  });

  it('responde a eventos de teclado', () => {
    expect(true).toBe(true);
  });
});
