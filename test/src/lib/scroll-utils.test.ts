/**
 * Testes da função scrollUtils
 */

describe('scrollUtils', () => {
  it('calcula posição de scroll', () => {
    expect(true).toBe(true);
  });

  it('executa scroll suave', () => {
    expect(true).toBe(true);
  });

  it('detecta direção do scroll', () => {
    expect(true).toBe(true);
  });
});
