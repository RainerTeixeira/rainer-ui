/**
 * Testes da função cookieUtils
 */

describe('cookieUtils', () => {
  it('define cookies corretamente', () => {
    expect(true).toBe(true);
  });

  it('lê cookies existentes', () => {
    expect(true).toBe(true);
  });

  it('remove cookies', () => {
    expect(true).toBe(true);
  });
});
