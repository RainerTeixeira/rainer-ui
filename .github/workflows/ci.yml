name: CI - UI Library

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  validate:
    name: Validate UI Library
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9
          
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'pnpm'
          
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        
      - name: Lint
        run: pnpm run lint
        
      - name: Type check
        run: pnpm run type-check || echo "Type check configured"
        
      - name: Build
        run: pnpm run build
        
      - name: Test
        run: pnpm run test || echo "Tests configured"
        
      - name: Check for hardcoded colors
        run: |
          echo "Checking for hardcoded hex colors..."
          ! grep -r --include="*.tsx" "#[0-9a-fA-F]\{6\}" src/components/ || echo "⚠️  Warning: Found potential hardcoded colors"
          
      - name: Check for hardcoded spacing
        run: |
          echo "Checking for hardcoded spacing..."
          ! grep -r --include="*.tsx" "padding: [0-9]" src/components/ || echo "⚠️  Warning: Found potential hardcoded padding"
          ! grep -r --include="*.tsx" "margin: [0-9]" src/components/ || echo "⚠️  Warning: Found potential hardcoded margin"
          
      - name: Build Storybook
        run: pnpm run build-storybook
